# Claudex Configuration

# Optional: Server port (default: 8080)
# PORT=8080

# Optional: Internal API key. When set, /v1/* routes require an API key via
# "Authorization: Bearer <key>" or "x-api-key: <key>". Leave unset to keep the
# API open (default).
# CLAUDEX_API_KEY=change-me

# Optional: Log level (default: info)
# LOG_LEVEL=debug

# Optional: Request timeout in seconds (default: 600)
# REQUEST_TIMEOUT=600

# Note: ANTHROPIC_API_KEY is no longer required.
# Claudex uses the Claude CLI which authenticates via ~/.claude credentials.
